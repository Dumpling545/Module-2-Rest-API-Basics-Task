dependencies {
    implementation group: 'org.postgresql', name: 'postgresql', version: postgresqlDriverVersion
    implementation group: 'org.springframework', name: 'spring-jdbc', version: springVersion
    implementation group: 'com.zaxxer', name: 'HikariCP', version: hikariConnectionPoolVersion
    implementation project(':model')

    testImplementation group: 'com.h2database', name: 'h2', version: h2DatabaseVersion
}