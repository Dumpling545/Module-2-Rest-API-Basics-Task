dependencies {
    api project(':service')

    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation project(':model')
}
bootJar {
    enabled = false
}
jar {
    enabled = true
}
